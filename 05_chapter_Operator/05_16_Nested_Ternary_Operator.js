// Multiple Condition

let age = 17;
//   age > 18 -> he will goa, else not else
// drink > 25  yes, else no 
let is_rohit_enjoy = age > 18 ? (age >26 ? "Drink" : "No") : false;
console.log(`rohit can drink ?: ${is_rohit_enjoy}`);