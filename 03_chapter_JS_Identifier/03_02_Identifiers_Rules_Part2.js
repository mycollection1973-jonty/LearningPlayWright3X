var name = "Rohit";

var firstName = "Rohit";
var This_is_a_very_long_name_variable = "Rohit";
var lastName = "Rawat";

// Naming Conventions (Cases)
// ============================================
// 1. camelCase (standard for JS variables and functions) -- will use

let userName = "camel";
let totalPrice = 99.99;
let isLoggedIn = true;

// 2. PascalCase (standard for JS classes and constructors)  -- will use
let UserProfile = "PascalCase";
let ShoppingCart = "class name style";

// 3. snake_case (underscore separated)
let user_name = "snake_case";
let total_price = 49.99;
let is_logged_in = true;

// 4. SCREAMING_SNAKE_CASE (constants) -- will use
const MAX_SIZE = 100;
const API_KEY = "abc123";
const DATABASE_URL = "localhost";

// 5. Hungarian Notation (prefix with type - older style)
let strName = "string prefix";
let bActive = true;
let nCount = 5;
let arrItems = [];

let strFirstName = "Rohit";